# External files

This directory is reserved for external source files that are not redistributed in this package.

If you have access to the legacy structured California DMV crash table used for weak-label validation, place it here and pass its path to:

`scripts/modeling/fit_rear_end_injury_models.py --legacy-xlsx path/to/file.xlsx`
